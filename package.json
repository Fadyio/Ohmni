{
  "name": "ohmni",
  "version": "0.1.0",
  "description": "Agent-Native Hardware Diagnostic Workbench",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "bun test",
    "test:chrome": "bun run scripts/test-chrome.ts",
    "typecheck": "tsc --noEmit",
    "check": "tsc --noEmit",
    "verify": "bun test && bun run typecheck && bun run build",
    "smoke": "bun run scripts/smoke.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "@types/node": "^22.13.0",
    "typescript": "^5.7.3",
    "vite": "^8.2.2"
  }
}
